/** Show low-spendable banner when ETB spendable drops below this. */
export const LOW_SPENDABLE_ETB_THRESHOLD = 7_500;

/** Days without export before backup nudge appears. */
export const BACKUP_REMINDER_DAYS = 14;

/** Days to hide backup nudge after dismiss. */
export const BACKUP_DISMISS_DAYS = 7;
